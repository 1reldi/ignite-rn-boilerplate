import { StyleSheet } from 'react-native';
//import {} from 'Themes';

export default StyleSheet.create({
  container: {
    flex: 1
  }
});
//cd node_modules/react-native/third-party/glog-0.3.4/ && ../../scripts/ios-configure-glog.sh && cd .. && cd .. && cd .. && cd ..
