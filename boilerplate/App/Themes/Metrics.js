//import { Dimensions, Platform } from 'react-native';

// Used via Metrics.baseMargin
const Metrics = {
  defaultHitSlop: value => ({
    top: value,
    left: value,
    right: value,
    bottom: value
  }),
  defaultMargin: {
    marginTop: 10,
    marginBottom: 10,
    marginLeft: 10,
    marginRight: 10
  }
};

export default Metrics;
